# Демки не отдаём напрямую по URL — только через getdemo.php (чтение с диска).
Options -Indexes
<IfModule mod_authz_core.c>
	Require all denied
</IfModule>
<IfModule !mod_authz_core.c>
	Order allow,deny
	Deny from all
</IfModule>
